# Authelia Configuration (for local testing)
# For GitHub Actions deployment, set these as GitHub Secrets instead

AUTH_DOMAIN=yourdomain.com
AUTH_USERNAME=admin
AUTH_PASSWORD=your-secure-password
AUTH_EMAIL=admin@yourdomain.com
